/**
 * Sun function for two numbers.
 *
 * @param {*} a - number
 * @param {*} b - number
 *
 * @returns Sum of `a` and `b`
 */
export const sum = (/** @type {number} */ a, /** @type {number} */ b) => a + b;
